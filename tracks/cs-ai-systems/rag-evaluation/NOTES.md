# Notes

- Treat `tracks/cs-ai-systems/rag-evaluation` as the active `/teach` workspace even when commands run from the repo root.
- `MODULE_CONTRACT.md` already exists and should steer assessment mix, output shape, and source choice.
- Keep all setup and teaching artifacts inside this module unless a root-level update is clearly required.
- Start with diagnosable evaluation cases before tool integration. The first useful skill is separating retrieval failure, generation failure, both, and dataset ambiguity.
- Do not create learning records until the user demonstrates understanding or discloses prior knowledge. The `learning-records/` directory exists for future records.
